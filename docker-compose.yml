version: "3.9"

services:
  app:
    build: .          # Dockerfile added in Phase 13
    ports:
      - "${PORT:-8080}:8080"
    env_file:
      - .env
    volumes:
      - db-data:/data
    environment:
      DB_PATH: /data/foodscheduler.db

  # Uncomment in Phase 12 when the Postgres adapter is ready.
  # postgres:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: foodscheduler
  #     POSTGRES_PASSWORD: foodscheduler
  #     POSTGRES_DB: foodscheduler
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - pg-data:/var/lib/postgresql/data

volumes:
  db-data:
  # pg-data:
